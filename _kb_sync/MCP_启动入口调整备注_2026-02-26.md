# MCP 启动入口调整备注（2026-02-26）

## 调整目标
- 只保留一个 MCP 启动入口，避免重复拉起 `node/cmd` 进程。
- 去掉 `npx` 包装，改为直接执行 `lark-mcp`。

## 本次改动
1. 修改 `.vscode/mcp.json`
- 原：定义了 `feishu` MCP server（会由 VS Code 侧启动）。
- 现：`servers` 置空，停用工作区级 MCP 启动入口。

2. 修改 `C:\Users\lishuobin\.codex\config.toml`
- 原：`command = "npx"`，且参数前缀为 `-y @larksuiteoapi/lark-mcp mcp ...`。
- 现：`command = "lark-mcp"`，参数改为 `mcp ...`（去掉 npx 包装）。

## 影响说明
- 现在由 Codex 作为唯一 MCP 启动入口。
- 进程链路会减少一层 `npx/cmd` 包装，通常能降低后台进程数量与抖动。
- 若系统 PATH 中不存在 `lark-mcp`，将导致 MCP 启动失败（需先安装并确保可执行）。

## 回滚方法
1. 恢复 `.vscode/mcp.json` 的 `feishu` server 配置。
2. 将 `C:\Users\lishuobin\.codex\config.toml` 改回：
   - `command = "npx"`
   - `args` 以 `"-y", "@larksuiteoapi/lark-mcp", "mcp"` 开头。
